# compress

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build compress` to build the library.

## Running unit tests

Run `nx test compress` to execute the unit tests via [Jest](https://jestjs.io).
