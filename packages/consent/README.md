# consent

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build consent` to build the library.

## Running unit tests

Run `nx test consent` to execute the unit tests via [Jest](https://jestjs.io).
